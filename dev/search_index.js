var documenterSearchIndex = {"docs":
[{"category":"section","location":"pattern_mining/dialects/#Mining-Dialects-—-def/match/emit-four-way-support","page":"Mining Dialects","text":"The Hyperon Pattern Miner's frequent-pattern core (abstract → support → mark-kept) realized on the MeTTa-IL def/match/emit pipeline surface, with the support operation computed in four agreeing dialects.","title":"Mining Dialects — def/match/emit + four-way support"},{"category":"section","location":"pattern_mining/dialects/#The-def/match/emit-frequent-pattern-miner","page":"Mining Dialects","text":"Candidate patterns are generated by an abstraction pipeline written in the def/match/emit surface (see [MeTTa-IL]; lowered to MM2 exec per §9.2). Each def is a named stage; match binds inputs; emit produces the next stage's facts. Abstraction replaces a position with a _ wildcard (a literal marker — no fresh variable; MORK deduplicates identical candidates):\n\n(def abs-subj () (match (drink $s $o) (emit (cand (drink _ $o)))))\n(def abs-obj  () (match (drink $s $o) (emit (cand (drink $s _)))))\n\nmine_frequent(cs, abstract_pipeline, minsup) runs the pipeline, counts each candidate's support (distinct ground matches), and keeps those with support ≥ minsup. On the tutorial \"drink\" DB at minsup = 2:\n\n(drink Alice _) support=2   (drink Bob _) support=2\n(drink _ Coke)  support=2   (drink _ Tea) support=3\n\n(drink Carol _) (support 1) is dropped. Follow-ons (per the tutorial): build-specialization, conjunction expansion (do-conjunct → (and …)), and I-surprisingness ranking.","title":"The def/match/emit frequent-pattern miner"},{"category":"section","location":"pattern_mining/dialects/#Support-in-four-dialects-(they-all-agree)","page":"Mining Dialects","text":"Following §9's \"same algorithm, compare dialects\" methodology, the support op is computed four ways; all return identical counts on the same data (test_pattern_miner.jl, bench_dialects_and_speedup.jl):\n\nDialect Function Mechanism\nRaw-MeTTa interpreter pattern_support_interp (size-atom (collapse (match &self P P)))\nRelational (MeTTa-IL → MM2) pattern_support structural trie-dump wildcard match\nMORK trie query pattern_support_native space_query_multi on the comma-wrapped (, P)\nMORK-Miner §2.3 counter prefix_count_support O(1) read of an in-place prefix counter\n\n(drink $x Coke)       interp=2 scan=2 native=2 counter=- → AGREE\n(drink Alice $y)      interp=2 scan=2 native=2 counter=2 → AGREE\n(inherit $x American) interp=1 scan=1 native=1 counter=- → AGREE","title":"Support in four dialects (they all agree)"},{"category":"section","location":"pattern_mining/dialects/#Note-on-the-MORK-trie-query","page":"Mining Dialects","text":"pattern_support_native uses space_query_multi, which requires the pattern comma-wrapped as a (, pattern) conjunction (a bare pattern matches nothing — it processes a (, …) factor list; the convention is in space.rs dump_sexpr). It is correct but O(N) — it iterates rather than prefix-anchoring, so it does not provide the MORK-Miner speedup. That speedup comes from the in-place counter, documented in MORK-Native Miner.","title":"Note on the MORK trie query"},{"category":"section","location":"pattern_mining/dialects/#API","page":"Mining Dialects","text":"Full docstrings in the API Reference: mine_frequent, pattern_support, pattern_support_interp, pattern_support_native.\n\n[MeTTa-IL]: https://github.com/CognitiveSubstratesAI/Core/blob/main/src/standard/MeTTaIL.jl\n\n","title":"API"},{"category":"section","location":"pattern_mining/overview/#Pattern-Mining-—-Overview","page":"Overview","text":"Pattern mining discovers reusable structures (templates recurring across contexts) in an Atomspace — priming reasoning, suggesting subgoals, and compressing knowledge. MeTTaCore implements pattern mining at several layers, deliberately, to compare language dialects and substrate strategies on the same algorithm. This section documents each.","title":"Pattern Mining — Overview"},{"category":"section","location":"pattern_mining/overview/#Source-material","page":"Overview","text":"Three Goertzel documents (under docs/research/papers/Mork/) ground this work:\n\nDocument What it is\nPattern-Miner-Tutorial-MeTTa4 The Hyperon Pattern Miner algorithm: abstract → specialize → support → expand-conjunctions → I-surprisingness\nMORK-Miner A PathMap-native miner: prefix-locality, seed/grow by prefix, in-place support counters\nmetta-magic_v2 Framework context (metta_magic/PyMeTTa); the miner is one algorithm in a broader vision\n\nThe canonical reference implementations are cloned to ~/PRIMUS/dev-zone/: hyperon-miner (the authoritative MeTTa miner) and weighted-atom-sweep (the Weighted Atom Sweeps cognitive scheduler).","title":"Source material"},{"category":"section","location":"pattern_mining/overview/#The-\"three-dialects\"-methodology","page":"Overview","text":"The Scalable-MeTTa-Infrastructure spec (§9) implements the same Hyperon Pattern Miner in multiple language dialects in order to compare the variants — it is a measuring benchmark, not a set of competing miners. The pattern miner is chosen precisely because it is algorithmically simple, lives in the metagraph, and has a published tutorial. We follow that methodology: the support operation (the core stream op) is realized in four ways that all agree on the same data — see Mining Dialects.","title":"The \"three dialects\" methodology"},{"category":"section","location":"pattern_mining/overview/#WILLIAM-is-the-production-miner","page":"Overview","text":"MeTTaCore already ships WILLIAM — the papers-based frequent-subgraph miner (grounded mine-patterns primitive, wired into Core), which the Hyperon whitepaper (§7.2.2) uses for template-library creation. The miners documented here are the educational / comparison complement: they make the language and substrate layers explicit and measurable. For production frequent-subgraph mining, use WILLIAM.","title":"WILLIAM is the production miner"},{"category":"section","location":"pattern_mining/overview/#API-surface","page":"Overview","text":"All in src/standard/PatternMiner.jl (the def/match/emit pipeline surface it builds on is in MeTTaIL.jl):\n\nFrequent-pattern miner (def/match/emit → MM2): mine_frequent, pattern_support.\nSupport dialects: pattern_support_interp (raw-MeTTa), pattern_support (relational scan), pattern_support_native (MORK trie query).\nMORK-native prefix miner: prefix_support, mine_prefix_patterns, grow_prefix.\nMORK-Miner §2.3 in-place counters: PrefixCounter, prefix_insert!, prefix_counter, prefix_count_support.\n\nRunnable experiments: experiments/pattern_mining/ (demo_miners.jl, bench_dialects_and_speedup.jl). Tests: test/test_pattern_miner.jl (27/27).\n\n","title":"API surface"},{"category":"section","location":"gslt/#GSLT-Theory-Algebra","page":"GSLT Theory Algebra","text":"The GSLT theory front-end (src/standard/GSLT.jl) is the real MeTTa-IL bulk (per F1R3FLY/MeTTaIL's GSLT/.module representation): MeTTa-IL theories are built compositionally from parts. Theories flatten to a set of rewrites that feed the MeTTa-IL Lane.\n\nPRIMUS-native s-expr surface (the GSLT BNF term Mult . Elem ::= \"(\" Elem \"*\" Elem \")\" becomes the type-sig (: Mult (-> Elem Elem Elem)) — the s-expr is the syntax):\n\n(theory NAME ()|(extends P)|(union A B)\n  (params (p P) …)\n  (terms …) (equations …) (rewrites …) (replacements (=> Old New) …))","title":"GSLT Theory Algebra"},{"category":"section","location":"gslt/#The-theory-algebra","page":"GSLT Theory Algebra","text":"theory_flatten resolves the algebra into the complete terms/equations/rewrites:\n\nExtension — (extends P): inherit P's content, then add this theory's.\nUnion — (union A B …): merge all parents (e.g. a Rig = additive CommutativeMonoid ∪ multiplicative Monoid).\nReplacement — (replacements (=> Old New)): whole-token constructor rename across the inherited content (substring-guarded).\nParameterization — (params (p P)): a theory generic over its parameter theories; p defaults to P and is overridable at instantiation via theory_instantiate.\n\nValidated against the canonical upstream UnivAlg.module hierarchy (EmptySet → Monoid → CommutativeMonoid / Group → Rig), reproducing its constructor and equation sets exactly.","title":"The theory algebra"},{"category":"section","location":"gslt/#Running-a-theory","page":"GSLT Theory Algebra","text":"theory_run! flattens a theory's rewrites and runs them on native MORK (with saturate=true for recursive closure). theory_rewrites returns the flattened rewrite set; parse_theory / load_theories parse the surface.","title":"Running a theory"},{"category":"section","location":"gslt/#Equation-orientation","page":"GSLT Theory Algebra","text":"theory_orient_equations orients equations into rewrites where provably terminating: the RHS must have strictly fewer function symbols and no variable may occur more often in the RHS than in the LHS (non-duplication). Both halves are load-bearing — the symbol count is a measure on the rule, and substitution multiplies each variable's instance, so a duplicating rule such as (= (f (h $x)) (g $x $x)) grows the term (|Lσ| = 2 + |t| vs |Rσ| = 1 + 2|t|) even though the rule itself shrinks. Non-duplication subsumes the older \"no new variables\" test. Unit/simplification laws orient; commutativity / associativity / distributivity are flagged (they need AC-matching or an LPO/KBO reduction order — the deeper follow-on). Oriented rewrites feed metta_il_normalize.\n\n","title":"Equation orientation"},{"category":"section","location":"mettail/#MeTTa-IL-Lane","page":"MeTTa-IL Lane","text":"MeTTaCore supports two execution lanes over one MM2/MORK substrate: a CeTTa-style direct lane (interpreter-spec + MM2 router) and the F1R3FLY MeTTa-IL layered pipeline documented here (MeTTa → MeTTa-IL → MM2 → MORK). Both bisimulate against the interpreter-spec. A single entry, mc_run (see below), dispatches to the right lane by program form.\n\nThe MeTTa-IL lane (src/standard/MeTTaIL.jl) lowers a theory's rewrites to MM2 exec rules and runs them on the native MORK substrate — no surface→IR rewrite, no FFI. It is grounded in the actual F1R3FLY upstream (the GSLT .module representation + mettail-rust), not the scalable-infra paper's §9.1 sketch: the executable reduction relation is the rewrites Name : LHS ~> RHS, written (~> LHS RHS).","title":"MeTTa-IL Lane"},{"category":"section","location":"mettail/#Two-modes-(same-surface,-different-semantics)","page":"MeTTa-IL Lane","text":"~> has two legitimate execution interpretations; pick by workload:\n\nMode Function Semantics Use\nForward-derivation metta_il_run! additive: LHS present → derive RHS (kept) relational / Datalog (transitive closure, deductive queries)\nTerm reduction metta_il_normalize redex → contractum, with congruence (reduce in context) calculi (rho/lambda, equational reduction)\n\n(~> LHS RHS) lowers to MM2 (exec 0 (, LHS) (, RHS)) (metta_il_lower_rewrite).","title":"Two ~> modes (same surface, different semantics)"},{"category":"section","location":"mettail/#Recursive-closure-(saturation)","page":"MeTTa-IL Lane","text":"The single-step exec calculus does one generation, so recursive rewrites (a rewrite whose RHS head also appears in a body — e.g. transitive closure) don't close. Pass saturate=true to metta_il_run!: rewrites lower to KBSaturation forward rules (metta_il_lower_saturation) and run to fixpoint (value-deduped, so cyclic input terminates).","title":"Recursive closure (saturation)"},{"category":"section","location":"mettail/#Congruence","page":"MeTTa-IL Lane","text":"GSLT/mettail-rust spell out explicit congruence rules (let Src ~> Tgt in (Ctx Src) ~> (Ctx Tgt)) only because their relational backend can't reduce subterms. MeTTaCore builds congruence as a subterm-rewriting normalizer (metta_il_normalize): rewrite any subterm matching a base-rewrite LHS, innermost, to fixpoint — the subterm descent is the congruence.","title":"Congruence"},{"category":"section","location":"mettail/#The-def/match/emit-pipeline-surface-(§9.1-§9.2)","page":"MeTTa-IL Lane","text":"A higher-level surface for staged relational pipelines (e.g. pattern miners). Each def is a named stage; match binds inputs; when guards; emit produces the next stage's facts. Per the paper's own §9.2 lowering, each stage becomes (exec PRIORITY (, PATS GUARDS) (, EMITS)), PRIORITY = stage order:\n\n(def increment-count ()\n  (match (candidate $c $p) (count $c $p $n)\n    (emit (count $c $p (+ $n 1)))))\n\nmetta_il_lower_def / metta_il_lower_pipeline / metta_il_run_pipeline!. The Pattern Mining section uses this surface; see also the GSLT Theory Algebra, whose flattened rewrites feed this lane.","title":"The def/match/emit pipeline surface (§9.1 → §9.2)"},{"category":"section","location":"mettail/#Unified-entry-(mc_run)","page":"MeTTa-IL Lane","text":"Rather than calling each lane's function directly, mc_run is the single dual-track entry. It dispatches by program form — the lanes are different front-ends over the same MM2/MORK substrate, so which one runs is determined by what you wrote, not by an engine switch:\n\nflowchart TD\n    P[\"Program + data\"] --> MC{\"mc_run<br/>dispatch by form\"}\n    MC -->|\"(theory ...)\"| T[\"GSLT theory lane<br/>theory_run!\"]\n    MC -->|\"(def ...)\"| D[\"def/match/emit<br/>metta_il_run_pipeline!\"]\n    MC -->|\"(~&gt; ...)\"| R[\"rewrite lane<br/>metta_il_run!\"]\n    MC -->|\"exec / data / !\"| DIR[\"direct lane<br/>mm2_route!\"]\n    T --> MM2[\"MM2 exec rules\"]\n    D --> MM2\n    R --> MM2\n    DIR --> MM2\n    MM2 --> MORK[\"MORK / PathMap trie (ZAM zipper)\"]\n\nProgram contains Lane Runs\n(theory …) GSLT theory theory_run! (use theory=NAME to pick one)\n(def …) def/match/emit pipeline metta_il_run_pipeline!\n(~> …) rewrite metta_il_run! (saturate=true for recursive closure)\notherwise (exec/data/!) direct mm2_route!\n\nmode ∈ (:direct, :rewrite, :pipeline, :theory) forces a lane. The call returns (; lane, results) — the chosen lane plus its native result (a Vector{String} for the MeTTa-IL lanes; the route NamedTuple for :direct).\n\nmc_run(cs, facts, raw\"(~> (, (edge $x $y) (edge $y $z)) (trans $x $z))\")\n# → (lane = :rewrite, results = [\"(trans 0 2)\", \"(trans 1 3)\"])\n\n","title":"Unified entry (mc_run)"},{"category":"section","location":"pattern_mining/mork_miner/#MORK-Native-Miner-—-prefix-locality-in-place-counters","page":"MORK-Native Miner","text":"The MORK-Miner mines entirely inside MORK's PathMap index, exploiting prefix-tree locality: atoms stored as token-paths share byte-trie prefixes, so atoms with a common leading prefix are \"nearby\" and likely participate in the same pattern. This avoids global scans and subgraph isomorphism.","title":"MORK-Native Miner — prefix locality + in-place counters"},{"category":"section","location":"pattern_mining/mork_miner/#Prefix-locality-mining","page":"MORK-Native Miner","text":"Patterns are left-anchored prefixes (head t1 … tk _) — distinct from the any-position _ wildcards of the relational miner. Three stages, all PathMap operations:\n\nSeed — mine_prefix_patterns(cs, depth, minsup): group atoms by their leading depth tokens, count atoms under each prefix, keep the frequent ones.\nGrow — grow_prefix(cs, prefix, minsup): extend a frequent prefix one token deeper (prefix proximity).\nSupport — the count of atoms under a prefix.\n\nOn the \"drink\" DB, depth 2, minsup = 2: (drink Alice _):2, (drink Bob _):2 ((drink Carol _):1 and (inherit Alice _):1 dropped).","title":"Prefix-locality mining"},{"category":"section","location":"pattern_mining/mork_miner/#§2.3-in-place-counters-—-the-speedup","page":"MORK-Native Miner","text":"The MORK-Miner's key idea is that support is not computed by a scan or a query pass. Per §2.3:\n\n\"Whenever a new data item is stored, the miner increments an integer counter at each prefix node along its key path. The support of any pattern is obtained by reading the counter — no separate matching or counting pass is required.\"\n\nPrefixCounter realizes this: prefix_insert! increments a counter at every prefix of an atom's key path at insert time; prefix_count_support reads support in O(1).\n\npc = prefix_counter(data)                       # one-time storage pass (counters maintained at insert)\nprefix_count_support(pc, [\"drink\", \"Alice\"])    # O(1) read → 2\n\nBenchmark (bench_dialects_and_speedup.jl) — O(1) counter read vs an O(N) dump-scan for the same support:\n\nN (atoms) O(1) counter read O(N) scan speedup\n4,000 0.15 µs 21 ms 145,071×\n16,000 0.12 µs 106 ms 847,875×\n64,000 0.18 µs 419 ms 2,344,802×\n\nThe counter read is flat in N; the speedup grows with the dataset — the paper's \"orders of magnitude, no global scans,\" demonstrated.","title":"§2.3 in-place counters — the speedup"},{"category":"section","location":"pattern_mining/mork_miner/#A-correction-worth-recording","page":"MORK-Native Miner","text":"An earlier benchmark used a post-hoc space_query_multi/scan, found it O(N) and slower, and briefly concluded the speedup \"wasn't real / needed deep substrate work.\" That was our error — we had benchmarked the wrong mechanism (the \"separate counting pass\" §2.3 explicitly avoids), not the paper's algorithm. Implementing the actual in-place counter vindicated the claim. The standing lesson: when a measured result contradicts a serious paper, assume we erred and find it before doubting the claim.\n\nspace_query_multi_at (the prefix-anchored query variant) is for the multi-space \"spaces as prefixes\" scoping model — not a pattern-prefix counter — so it is not the right tool here; the in-place counter is.","title":"A correction worth recording"},{"category":"section","location":"pattern_mining/mork_miner/#API","page":"MORK-Native Miner","text":"Full docstrings in the API Reference: mine_prefix_patterns, grow_prefix, prefix_support, PrefixCounter, prefix_insert!, prefix_counter, prefix_count_support.\n\n","title":"API"},{"category":"section","location":"typing-conventions/#MeTTa-type-declarations-—-when-(and-when-not)-to-type","page":"MeTTa Typing Conventions","text":"MeTTa is gradually typed: type declarations are optional. Undeclared atoms have type %Undefined%, type-checking is opt-in (get-type / type-cast / match-types), and there is no automatic checking pass. So adding (: …) declarations buys little static safety unless you also invoke the checkers — it is mostly documentation and get-type recognition.\n\nHow heavily to type a file therefore depends entirely on which tier the file is in. Getting this wrong in either direction is a common mistake (Core over-typed lib/pln/pln_factor_graph.metta and reverted it — see the case study below).","title":"MeTTa type declarations — when (and when not) to type"},{"category":"section","location":"typing-conventions/#The-tier-rule","page":"MeTTa Typing Conventions","text":"Tier Examples Convention\nstdlib / prelude / grounded-op / FFI-binding src/standard/stdlib.metta; hyperon stdlib.metta; CeTTa stdlib.metta/mork.metta Fully typed. Every grounded/built-in op gets a (: op (-> …)) signature — the signature is the doc + type-check surface for an op with no (= …) rule.\ndomain-algorithm library lib/pln/, lib/ecan/, lib/quantale/, MOSES Light / bare. Bare symbolic constructors; reuse built-in types; at most a few pure numeric-helper signatures. No custom data types unless the algorithm is about types.","title":"The tier rule"},{"category":"section","location":"typing-conventions/#Why-—-the-evidence","page":"MeTTa Typing Conventions","text":"Cross-checked against the official MeTTa spec, hyperon-experimental, and CeTTa (2026-06):\n\nOfficial spec: declarations are presented as optional infrastructure; the spec's own examples are mostly bare (= …) rewrites. Explicit (: …) appears selectively (e.g. (: if (-> Bool Atom Atom $t)), where the meta-type Atom on the branches is evaluation-control- load-bearing — not cosmetic).\nhyperon-experimental: the stdlib (stdlib.metta, 1421 lines) declares 2 custom types (ErrorType, SpaceType); everything else is arrow signatures over built-ins. Real modules (json.metta, skel.metta) type their functions but add 0–1 custom types. Domain logic (b4_nondeterm.metta, the ML-integration sandboxes) ships untyped, bare.\nCeTTa: heavy typing lives only in stdlib.metta (prelude) and mork.metta (FFI handle layer). Real domain libs are nearly type-free — lib_pln.metta is 6 decls / 84 rules (numeric helpers only); clist.metta defines ClistNil/ClistCons and pattern-matches them yet declares zero types.\nDensity norm: domain-algorithm libraries sit at ~0–7 % declaration density (decls : rules). hyperon/CeTTa stdlib and the dependent-type chaining repo are the only high-density bodies — and those are stdlib or about types.","title":"Why — the evidence"},{"category":"section","location":"typing-conventions/#Guidance-for-a-new-domain-algorithm-library-(lib/**)","page":"MeTTa Typing Conventions","text":"Use bare symbolic constructors — (stv s c), (dtv μ n), (dpair a b). Do not mint (: STV Type) / (: dtv (-> Number Number DTV)). (Lowercase stv matches the upstream PLN-for-PeTTa representation.)\nReuse built-in types — Number, Bool, Atom, Expression, Symbol. Never wrap a built-in in a parallel custom type.\nOptionally sign a few pure numeric helpers (-> Number Number) (CeTTa lib_pln style) — purely as arity/domain documentation. This is the upper bound for a domain lib.\nDeclare a custom (: MyType Type) + typed constructors only when the algorithm is about types — typed pattern dispatch, GADT/dependent proof-search, Peano depth bounds. Numeric propagation, attention spreading, program evolution are not — leave them bare.\nPer-function signatures and exhaustive accessor types are non-idiomatic for this tier and give no checked safety (gradual typing, no auto-pass). Skip them.","title":"Guidance for a new domain-algorithm library (lib/**)"},{"category":"section","location":"typing-conventions/#When-typing-IS-load-bearing","page":"MeTTa Typing Conventions","text":"Type only where it changes behaviour or is the algorithm's substance:\n\nEvaluation control — declaring a function argument as meta-type Atom (vs an evaluated type) stops MeTTa pre-evaluating it. This is the if case; it is semantic, not cosmetic.\nGrounded / stdlib ops — sign them in stdlib.metta. Core's comparison and math ops (< > <= >= sqrt log max min exp abs pow clamp) were %Undefined%, a faithfulness gap vs CeTTa/PeTTa; they are now typed (Number → Bool / Number → Number), which the conformance matrix confirms is safe and which backs any library signature that flows through them.\nGADT / dependent dispatch — declare the type hierarchy; the types drive the computation.","title":"When typing IS load-bearing"},{"category":"section","location":"typing-conventions/#Case-study:-lib/pln/pln_factor_graph.metta","page":"MeTTa Typing Conventions","text":"The PLN factor-graph layer (numeric demand propagation — not about types) was given a full typing pass: 16 custom data types + ~46 function/accessor signatures (~38 % density). The cross-check above showed this applied the stdlib tier's convention to a domain lib, 5× the ~7 % sibling norm. It was reverted to bare constructors. The one part kept was the stdlib.metta op typing — that genuinely belongs to the stdlib tier and closed a real gap. The lesson: match the file's tier; for a domain lib, bare is correct.\n\n","title":"Case study: lib/pln/pln_factor_graph.metta"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"Complete reference for all public symbols of MeTTaCore. The narrative guides explain how these fit together: Pattern Mining, MeTTa-IL Lane, GSLT Theory Algebra, and Dialect gaps.\n\nModules = [MeTTaCore, MeTTaCore.Interpreter, MeTTaCore.Interpreter.StandardMeTTa]\nOrder   = [:module, :type, :function, :macro, :constant]\n\n","title":"API Reference"},{"category":"section","location":"query-optimization/#MeTTa-MM2-Trie-Join:-Query-Execution-Optimization","page":"Query Optimization (MeTTa→MM2→Trie-Join)","text":"This page documents the two-day arc (2026-06-26 → 2026-06-27) that took MeTTa rules from the typed interpreter down to the MORK byte-calculus substrate and then made that substrate's conjunctive-query execution asymptotically faster.\n\nIt has two halves:\n\nThe MeTTa → MM2 bridge (2026-06-26) — lowering relational (= …) / (match …) MeTTa rules into MM2 exec atoms that run as pattern-directed metagraph rewriting on the MORK substrate, plus the exec-calculus correctness fixes and a permanent differential gate.\nTrie-join + projection pushdown (2026-06-27, ADR-056) — replacing the substrate's naive ProductZipper conjunction join with trie-native fast paths, and skipping path enumeration entirely when a query projects to its endpoints.\n\nflowchart LR\n    A[\"MeTTa source<br/>(= …) / (match …)\"] -->|\"MM2 bridge<br/>(Core: MM2Router)\"| B[\"MM2 exec atoms<br/>(exec sys (, PAT) (, TMPL))\"]\n    B -->|\"space_metta_calculus!\"| C{\"_space_query_multi_inner!<br/>(conjunction join)\"}\n    C -->|\"empty-tail / binary / chain\"| D[\"trie-join fast paths<br/>(P1 / P2 / P3)\"]\n    C -->|\"other shapes\"| E[\"ProductZipper<br/>(fallback, unchanged)\"]\n    D --> F{\"space_transform_multi_multi!<br/>(template + sink)\"}\n    E --> F\n    F -->|\"chain + endpoint projection\"| G[\"composition<br/>(P4-B)\"]\n    F -->|\"any set-sink projection\"| H[\"output dedup<br/>(P4-A)\"]\n\nEverything below is additive with full fallback: any pattern/shape an optimization does not recognize falls through to the unchanged path. The MM2 corpus differential (test/integration/mm2_corpus_differential.jl) stayed byte-identical through the entire arc.\n\n","title":"MeTTa → MM2 → Trie-Join: Query Execution Optimization"},{"category":"section","location":"query-optimization/#Part-1-—-The-MeTTa-MM2-bridge-(2026-06-26)","page":"Query Optimization (MeTTa→MM2→Trie-Join)","text":"MM2 is MORK's exec calculus: an atom (exec <sys> (, PAT…) (, TMPL…)) means match PAT against the space, instantiate TMPL per match, apply the sink — pattern-directed metagraph rewriting (not Datalog). The bridge auto-lowers the relational subset of MeTTa into these atoms so they execute on the substrate instead of the tree-walking interpreter.","title":"Part 1 — The MeTTa → MM2 bridge (2026-06-26)"},{"category":"section","location":"query-optimization/#Bridge-components-(Core-—-src/standard/MM2Router.jl,-DualTrack.jl)","page":"Query Optimization (MeTTa→MM2→Trie-Join)","text":"Function Role\nmm2_lower_equals (= LHS RHS) → (exec 0 (, LHS) (, RHS)) (relational rules only)\nmm2_lower_match (match &space pat res) → (exec 0 (, pat) (, res))\nmm2_is_relational guard — rejects rules with grounded ops / special forms, and enforces the MORK byte-Expr limits (arity 63 / 64 vars)\ntyped_atom_to_expr typed Atom → MORK s-expr — the live-eval handoff converter\nmm2_lane_saturate! recursive Datalog fixpoint driver (full transitive closure)\nmc_closure! / !(mork-closure) opt-in grounded op invoking the substrate route from MeTTa source\n\nThe boundary is forward-rewriting closure ≠ reduction-to-normal-form: the MM2 lane is sound only for forward-derivation (relational) workloads; grounded/control-flow rules stay in the interpreter. This was cross-checked against five reference MeTTa implementations (CeTTa, PeTTa, MeTTa-Compiler/F1R3FLY, MeTTaIL) — the F1R3FLY reference independently confirms the reduction-vs-closure boundary.","title":"Bridge components (Core — src/standard/MM2Router.jl, DualTrack.jl)"},{"category":"section","location":"query-optimization/#Exec-calculus-correctness-fixes-(MORK-—-src/kernel/Space.jl)","page":"Query Optimization (MeTTa→MM2→Trie-Join)","text":"Differential testing against the upstream mork binary surfaced three real bugs:\n\nCommit Bug Fix\n8f0d182 O-sink set difference was order-dependent RemoveSink now accumulates across matches → removes applied after adds\n7908fbd nested-exec variable hygiene — template vars collided with pattern vars template NewVars number past the pattern's introduced-var count (oi)\ne59a16b _pat_overlaps_exec_prefix scanned the whole buffer → false-positive on the template bound the scan to the pattern's own span\n\nThese are locked in by the MM2 corpus differential gate (eac6138), which runs each MM2_Structuring_Code tutorial program through space_metta_calculus! and asserts the exact upstream atom set (catching output-level bugs that a halting check would miss).\n\n","title":"Exec-calculus correctness fixes (MORK — src/kernel/Space.jl)"},{"category":"section","location":"query-optimization/#Part-2-—-Trie-join-fast-paths-(2026-06-27)","page":"Query Optimization (MeTTa→MM2→Trie-Join)","text":"The MORK conjunction join (_space_query_multi_inner!) was a naive Cartesian product: each factor of (, f1 f2 …) traverses the whole subtrie independently, and unification filters after enumerating each k-tuple — cost ≈ Nᵏ, order-invariant. The MORK author (Adam Vandervorst) blessed the trie-join as the asymptotic fix (\"introduces unions where product factors are independent\"). Three shape-specific fast paths now precede the ProductZipper, all in src/kernel/TrieJoin.jl:\n\nPhase Shape Mechanism Commit\nP1 empty-tail (p $x)(q $x) (unary, one shared var) pmeet of the relations' arg-value subtries 335d940 + c474d9c\nP2 binary (edge $x $y)(edge $y $z) (one shared var, free tails) key-rotation: re-key each factor by the join var, per-key tail-product 9e2064d\nP3 strict chain (edge $x0 $x1)…(edge $x_{k-1} $xk), k≥3 recursive depth-first streaming join (no intermediate materialization) 593246d","title":"Part 2 — Trie-join fast paths (2026-06-27)"},{"category":"section","location":"query-optimization/#Benchmark-results-(vs-ProductZipper,-warm)","page":"Query Optimization (MeTTa→MM2→Trie-Join)","text":"Phase Workload ProductZipper Trie-join Speedup\nP1 2 unary relations, N=400 4256 ms ~6 ms ~680×\nP2 layered DAG 2-hop, L=4 W=20 (800 edges) 3481 ms 0.74 ms ~4,730×\nP3 layered DAG 3-hop, L=4 W=7 (147 edges) 21,205 ms 0.19 ms ~111,000×\n\nThe speedup widens with size — ProductZipper is Nᵏ/|E|ᵏ, the trie-join is roughly linear. P3's 21-second ProductZipper on a 147-edge graph is the |E|³ catastrophe the streaming join eliminates.\n\n","title":"Benchmark results (vs ProductZipper, warm)"},{"category":"section","location":"query-optimization/#Part-3-—-Projection-pushdown-(2026-06-27)","page":"Query Optimization (MeTTa→MM2→Trie-Join)","text":"End-to-end through the exec calculus, a projecting template surfaced a second inefficiency: e.g. (exec 0 (, (edge $x $y)(edge $y $z)(edge $z $w)) (, (reach3 $x $w))) enumerates all W⁴ 3-paths but the output (reach3 $x $w) only depends on the endpoints — deduped to W² atoms by the set sink. Two variants address this in space_transform_multi_multi!:\n\nVariant Mechanism Scope Speedup Commit\nA — dedup-at-output skip the redundant idempotent set_val_at! for repeated outputs; adaptive gate (disable if no dups in first 512 matches) any projecting set-sink exec ~2.4× 4913d32\nB — composition compute the W² distinct endpoint pairs by relational composition, apply the template once per pair (skips path enumeration) strict chain + template projects to endpoints ~290× 7e8763e\n\nVariant A is always correct (idempotent set semantics) so its projection check is pure performance. Variant B is the asymptotic win: on the W=20 reach3 e2e it cuts 2753 ms → 9.5 ms (W² result vs W⁴ enumeration). B correctly does not fire when a template uses an intermediate var ((pathy $x $y $w)), or for non-chain / multiplicity-sensitive O-sink / anchored execs.\n\n","title":"Part 3 — Projection pushdown (2026-06-27)"},{"category":"section","location":"query-optimization/#Test-scripts-and-reproduction","page":"Query Optimization (MeTTa→MM2→Trie-Join)","text":"Test What it asserts Location (MORK)\nTrieJoin P1/P2/P3 (21 tests) each fast path ≡ ProductZipper / hand-computed truth, across overlap/disjoint/subset/identical/chain shapes test/integration/trie_join.jl\nProjection pushdown A (2) set-sink dedup preserves the atom set; non-projecting auto-disables test/integration/trie_join.jl\nP4-B projection composition (2) chain endpoint projection ≡ full exec; intermediate-var template does NOT fire B test/integration/trie_join.jl\nMM2 corpus differential (30) every MM2_Structuring_Code idiom matches the upstream mork atom set (byte-identical throughout the arc) test/integration/mm2_corpus_differential.jl\n\nRun the full MORK suite (warm REPL):\n\ncd MORK && printf 'include(\"test/runtests.jl\"); exit()\\n' | julia --project=. -i tools/repl.jl\n\nFinal state: 1808 passed, 0 failed (the one Aqua persistent_tasks error is an environment subprocess-spawn limitation, unrelated). Full design, safety conditions, and per-phase gate measurements are in ADR-056 (docs/architecture/ADR-056_zam_cardinality_join_planning.md).","title":"Test scripts & reproduction"},{"category":"section","location":"query-optimization/#Real-workload-validation-(FlyWire-connectome,-2026-06-27)","page":"Query Optimization (MeTTa→MM2→Trie-Join)","text":"The benchmarks above use synthetic layered DAGs with binary (edge $x $y) relations. Validating against the real FlyWire connectome info-flow workload (MORK/examples/connectome/) surfaced two limitations that the synthetic shapes could not:\n\nThe info-flow algorithm uses single-source anchored scans, not joins. Its hot query is (match &self (syn $r $p $c) …) pinned by the post-neuron, folded through a ratio threshold (reached-in / total-in ≥ 0.3) with grounded ops. A conjunction needs ≥2 factors, so the trie-join fast paths do not fire — the workload's speed comes from by-post trie-prefix anchoring (InfoFlowFast.metta), an orthogonal optimization.\nConnectome relations are ternary; the classifiers require binary. The synapse relation is (syn pre post count) (arity-4 atom, 3 args). _classify_binary_join / _classify_chain require exactly arity-3 factors (head + 2 args), so even a connectome motif query (, (syn $a $b $w)(syn $b $c $w2)) does not fire P2/P3 (confirmed: ternary → false, binary (edge …) → true).\n\nConclusion: the trie-join is correct and large on binary path/join workloads, but does not yet apply to raw connectome queries. The unlocking extension is arity-N factor support — let the join variable sit at any argument position with the remaining args (e.g. the synapse weight) carried as tails. This is a clean, scoped follow-on (generalize the arg-split from 2 to N and locate the shared var's position), after which connectome motif/path queries would fire the existing composition machinery. (Until then, a binary projection (edge a b) derived from (syn a b w) fires P2/P3 directly.)","title":"Real-workload validation (FlyWire connectome, 2026-06-27)"},{"category":"section","location":"query-optimization/#What-is-*not*-built-(deliberate,-per-the-MORK-author's-guidance)","page":"Query Optimization (MeTTa→MM2→Trie-Join)","text":"Non-chain k≥3 joins (star / general join graphs) — would need a runtime-data-driven join-order planner, which upstream found \"no satisfying solution without runtime data.\" Left to ProductZipper.\nIndependent-factor union — a no-op for the exec-calculus consumer (the result is the full product, and the effect contract must enumerate each tuple regardless).\n\n","title":"What is not built (deliberate, per the MORK author's guidance)"},{"category":"section","location":"dialect-gaps/#Dialect-gaps-—-porting-PeTTa-targeted-algorithms","page":"Dialect gaps (porting PeTTa-targeted algorithms)","text":"MeTTaCore is the Julia MeTTa-on-MORK runtime, faithful to hyperon-experimental. That faithfulness is load-bearing: the conformance suite and the unit corpus only mean something because Core matches hyperon's evaluation semantics. So the governing rule when porting an algorithm written for another MeTTa implementation is:\n\nwarning: Never change Core's evaluator to match another dialect\nWhen a ported algorithm misbehaves, the fix lives in the port (.metta), not in Core. Bending Core toward another dialect would make it a worse copy of that dialect and an unfaithful hyperon — and would void the conformance guarantees.\n\nThis page captures a concrete, recurring trap — PeTTa-targeted algorithms — so future ports recognize it immediately.","title":"Dialect gaps — porting PeTTa-targeted algorithms"},{"category":"section","location":"dialect-gaps/#Why-PeTTa-differs","page":"Dialect gaps (porting PeTTa-targeted algorithms)","text":"PeTTa transpiles MeTTa to Prolog. That gives it semantics that are PeTTa's distinguishing characteristic, not a missing capability in Core. The canonical example:\n\n filter-atom's predicate is applied to …\nPeTTa bound elements (Prolog include/3)\nhyperon the free (sealed) variable — cascades if the predicate has a computation body\nCore the free var too — faithful to hyperon\n\nSo a predicate written in PeTTa's idiom — e.g. isLiteral calling (car-atom $e) — works on PeTTa (where $e is a bound atom) but cascades / errors on both hyperon and Core (where the stdlib filter-atom rule evaluates it against a free var, and car-atom $var is undefined).","title":"Why PeTTa differs"},{"category":"section","location":"dialect-gaps/#The-three-oracle-diagnostic","page":"Dialect gaps (porting PeTTa-targeted algorithms)","text":"When a PeTTa-targeted algorithm misbehaves on Core, run the same program on three runtimes (all installed locally — see the oracle setup) and read the pattern:\n\nResult Diagnosis Fix\nfails on Core only, works on hyperon a Core bug — Core diverges from its spec fix Core\nfails on Core and hyperon, works on PeTTa a dialect gap — Core is faithful; the algorithm relies on PeTTa-specific (Prolog) semantics dialect-adapt the algorithm, not Core\n\nThe second row is the trap. \"Fails on Core\" alone is ambiguous; bringing hyperon in as the third leg is what disambiguates it. If Core fails identically to hyperon, Core is exonerated — it's faithful, and PeTTa is the odd one out.\n\nnote: Worked example — MOSES\nThe MOSES port (a 1:1 port of iCog's PeTTa-targeted metta-moses) hit exactly this. The upstream filter-atom/isLiteral idiom failed on both hyperon and Core (cascade) and worked on PeTTa. That \"failed on both\" was the good outcome — it proved the dialect gap rather than a Core bug. Adapted MOSES-side (not in Core) → test_moses 227/227, and MOSES learns the target programs end-to-end.","title":"The three-oracle diagnostic"},{"category":"section","location":"dialect-gaps/#How-to-dialect-adapt-(fix-patterns)","page":"Dialect gaps (porting PeTTa-targeted algorithms)","text":"Rewrite the PeTTa idiom into a hyperon-faithful form that produces the same result:\n\nUse Core's grounded, element-wise templates — (filter-atom $list $e (pred $e)), (map-atom $list $e (f $e)), (foldl-atom $list $init $acc $e (g $acc $e)). These apply the body over the bound elements (the include/3 behaviour), never the free var.\nMake predicates free-var-safe — guard any structural call with (== (get-metatype $e) Symbol) (or a case) before car-atom/cdr-atom, so the predicate never errors on a variable.\nInline / function-wrap the body where a bare rule-predicate doesn't survive (inline grounded expression, function-wrapped predicate, or a direct-value body).\nVerify each rewrite against the hyperon oracle — hyperon is the arbiter of \"faithful\"; PeTTa tells you only what the algorithm targeted.","title":"How to dialect-adapt (fix patterns)"},{"category":"section","location":"dialect-gaps/#Other-divergence-classes-to-watch","page":"Dialect gaps (porting PeTTa-targeted algorithms)","text":"The same \"Core is faithful; adapt the port\" discipline applies beyond filter-atom:\n\nTrue/False are symbols, not grounded booleans — check via get-metatype … Grounded / symbol comparison, per hyperon's idiom.\nNon-determinism / set-ops — PeTTa/MeTTaLog (collapse (filter … (superpose $x))) fan-out does not fork through application in Core's eager evaluator; port set-ops to the grounded filter-atom/map-atom + union-atom/subtraction-atom templates.\nHigher-order list ops — Core eagerly reduces a bare MeTTa-defined fn symbol passed as an arg and won't apply a var-head ($f x); use the grounded template ops instead.\nlet vs let* — plain let does not destructure a tuple pattern; only let* does.","title":"Other divergence classes to watch"},{"category":"section","location":"dialect-gaps/#The-takeaway","page":"Dialect gaps (porting PeTTa-targeted algorithms)","text":"A PeTTa-aimed package will have dialect divergences by construction — that's PeTTa being PeTTa, not Core being broken. Run the three-oracle diagnostic on any misbehavior, fix it in the port's .metta, keep it verified against hyperon, and leave Core's evaluator faithful.\n\n","title":"The takeaway"},{"category":"section","location":"backends/#Execution-Backends","page":"Execution Backends","text":"MeTTaCore is not a single interpreter — it is a front-end over several execution engines, with the long-term design routing each workload to the engine that fits its computational grain. This page documents what Core integrates today and where the architecture is heading.\n\nArchitectural basis: the cross-cutting spec docs/specs/execution_model_architecture.md (\"engine-per-workload\"). Status there: decided (analysis); first step = measure before building.","title":"Execution Backends"},{"category":"section","location":"backends/#Engine-per-workload-routing","page":"Execution Backends","text":"The tree-walking interpreter is the root performance cost (re-match / re-dispatch per node). The model shrinks it to a small reflective meta-kernel and routes the hot paths to specialized engines:\n\nWorkload Engine Status in Core\nsymbolic multi-leg joins / pattern matching / head dispatch ZAM / MORK — the Zipper Abstract Machine (the PathMap zipper) ✅ integrated (the substrate)\nwhole-program optimization (join-order, saturation, MM2 lowering) MorkSupercompiler (tier-2) ✅ integrated (sc_execute!)\nbackward goal-directed tabled recursion (SLG + well-founded negation) Prolog (SWI) now → native tabling-over-ZAM later ⏳ exploration / spike — not wired into Core\nnumeric / typed hot kernels native (FabricPC / Reactant / typed-Julia) external packages\nmeta / control tail (function/return, type-cast, reflection) small interpreted meta-kernel the current Interpreter\n\nflowchart LR\n    W[\"Workload\"] --> Q{\"computational grain\"}\n    Q -->|\"symbolic joins / match\"| ZAM[\"ZAM / MORK zipper<br/>(integrated)\"]\n    Q -->|\"whole-program opt\"| SC[\"MorkSupercompiler<br/>sc_execute! (integrated)\"]\n    Q -->|\"backward tabled recursion\"| PL[\"Prolog SWI<br/>(spike, not wired)\"]\n    Q -->|\"numeric / typed kernels\"| NK[\"FabricPC / Reactant\"]\n    Q -->|\"meta / control tail\"| IK[\"Interpreter meta-kernel\"]","title":"Engine-per-workload routing"},{"category":"section","location":"backends/#mc_run-—-the-execution-stack-(verified-from-code)","page":"Execution Backends","text":"The routing above is the model; mc_run (DualTrack.jl) is the implementation. Traced through the code (not the comments), the load-bearing fact is that there is one execution engine — space_metta_calculus!, the MM2 exec-calculus on the MORK trie. Every run reaches it: the Direct lane routes there directly (mm2_route! → space_metta_calculus!, MM2Router.jl:159), and the MorkSupercompiler execute! wraps it with whole-program stages but ends in the same call (SCPipeline.jl:451). So the MORK execution layer is not optional — it is the shared engine. What is tunable is only the execute! preprocessing, read straight from the SCOptions() constructor: stats=true, plan=true, decompose=true, saturate=false by default, engaged via supercompile=true (Direct) or saturate=true (MeTTa-IL).\n\nflowchart TD\n    MC{{\"mc_run · MM2 router<br/>dispatch by form · DualTrack.jl\"}}\n    MC -->|\"direct\"| DIR[\"DIRECT lane<br/>mm2_route!\"]\n    MC -->|\"~&gt; / def / theory\"| MIL[\"MeTTaIL lanes<br/>rewrite / pipeline / theory\"]\n    ITP[\"Interpreter · StandardMeTTa<br/>234/234 + LeaTTa 270/270<br/>(co-equal front-end + fallback)\"]\n\n    DIR --> LOW\n    MIL --> LOW[\"lower → MM2 (exec …) atoms<br/>mm2_lower_equals · :reduction / :relational\"]\n\n    subgraph SC[\"MorkSupercompiler — execution layer (NOT optional)\"]\n      direction TB\n      PRE[\"whole-program stages · execute! (tunable)<br/>stats · plan! · decompose (_sc_tmp) · [saturate] · [drive]\"]\n      ENG([\"space_metta_calculus! — MM2 exec-calculus<br/>EVERY run goes through this engine\"])\n      PRE --> ENG\n    end\n\n    LOW -->|\"default\"| ENG\n    LOW -. \"supercompile= / saturate=\" .-> PRE\n    ITP -. \"intended — code: scratch space today\" .-> ENG\n\n    ENG --> STORE[(\"MORK Space / PathMap<br/>content-addressed trie + .act\")]\n\n    classDef front fill:#eef0fe,stroke:#4f46e5,color:#312b90;\n    classDef eng fill:#dff0ed,stroke:#0f766e,color:#0a4f49;\n    classDef pre fill:#f2ecfd,stroke:#7c3aed,color:#4c1d95;\n    classDef itp fill:#e9f5ec,stroke:#15803d,color:#0d4d24;\n    classDef store fill:#e6f4f2,stroke:#0f766e,color:#0a4f49;\n    class DIR,MIL,LOW front;\n    class ENG eng;\n    class PRE pre;\n    class ITP itp;\n    class STORE store;\n\nThe fast lane (mc_run + MM2 router) dispatches by form into the Direct lane or the MeTTa-IL lanes; each lowers to MM2 (exec …) atoms that run on the one engine. The execute! stages (plan! / decompose → _sc_tmp join-staging / saturate) are whole-program preprocessing around that engine, cleaned up afterward (_cleanup_sc_tmp!, SCPipeline.jl:457). The Interpreter is a co-equal front-end and the fallback; routing its output through the engine is the intended design — in the current code (_mc_fallback_eval, DualTrack.jl:32) it evaluates on a scratch Interpreter.Space and does not yet pass through space_metta_calculus! (the dashed edge). The earlier \"~5–30× slower / materializes\" figure was a stale comment, not a measured property of this path — omitted here pending a benchmark.","title":"mc_run — the execution stack (verified from code)"},{"category":"section","location":"backends/#Lineage-—-what-the-direct-lane-inherited-from-CeTTa-(and-what-it-didn't)","page":"Execution Backends","text":"Cross-checked against CeTTa's actual C source (eval.c, mm2_lower.c, compile.c, main.c). The direct lane is often called \"CeTTa-style,\" but the precise picture is: it stands on shared upstream MORK + Hyperon-minimal machinery that CeTTa also uses, then extends it in ways CeTTa does not.\n\nShared upstream (both Core and CeTTa inherit it — it is MORK / Hyperon, not one from the other):\n\nThe MM2 exec vocabulary (exec <priority> <pattern> <template>) with I / ACT / BTM / O / + / - / , is upstream MORK (MORK/kernel/src/main.rs, MORK_TUTORIAL.md) — Core's (exec 0 (I L)(O (+R)(-L))) uses exactly those tokens.\nspace_metta_calculus! is the upstream Space::metta_calculus — the same fixpoint-over-trie exec-stepping CeTTa invokes over FFI.\nThe minimal-MeTTa interpreter skeleton (type-driven laziness, special forms controlling their own arg evaluation, the grounded-vs-equation split): Core's Interpreter.jl mirrors CeTTa's metta_eval / metta_call — both trace to Hyperon's minimal-MeTTa spec. Both also table equation queries.\n\nCore extended (not present in CeTTa):\n\nAuto-compilation of (= L R) → MM2 exec (mm2_lower_equals, modes :reduction / :relational). The big one: CeTTa does not lower equations — it treats MM2 as a separately authored surface (.mm2 / --lang mm2) where a human writes exec rules, and its mm2_lower maps = to an equality guard (mm2_guard_eq), not an equation→exec rewrite. Core compiles ordinary MeTTa equations onto the exec-calculus automatically.\nA runtime per-form router (mc_run) vs CeTTa's static up-front engine choice (CLI / --lang / suffix).\nMM2 / MORK as the primary path with the interpreter as fallback + bisimulation oracle — the inverse of CeTTa, where the tree-walk interpreter is primary and MM2 is the side surface.\n\nCeTTa has that the direct lane does not: an AOT LLVM emitter (compile.c, --compile: emits LLVM IR text then exits — and even that delegates back to metta_eval at runtime). Core does no native codegen in this path.\n\nSo \"the direct lane mirrors CeTTa\" is imprecise: the piece most directly shared with CeTTa is the interpreter (both are the Hyperon-minimal reducer); the direct lane itself is a Core-original auto-compiler of MeTTa equations onto the shared MORK MM2 substrate.","title":"Lineage — what the direct lane inherited from CeTTa (and what it didn't)"},{"category":"section","location":"backends/#Three-orthogonal-layers-—-substrate-vs-engines-vs-cognitive-processes","page":"Execution Backends","text":"\"Engine-per-workload\" is a statement about the middle layer only. Keep three axes distinct (collapsing them is a recurring framing error):\n\nCognitive processes — forward-chaining, backward-chaining (PLN demand / tabling), abduction, MOSES evolution, ECAN attention, concept blending. These are distinct reasoning paradigms that synergize through the shared AtomSpace (OpenCog \"cognitive synergy\"), not interchangeable backends. Promote them as first-class modes.\nExecution engines — ZAM zipper, MorkSupercompiler, native-tabling-over-PathMap, numeric kernels. Semantics-preserving executors chosen by the γ̂ diagnostic. \"Backend\" is the right word only here — it's about speed, not meaning.\nSubstrate — MORK / PathMap: the one shared metagraph memory + base rewriting/query. It is neither a cognitive architecture nor \"a backend among others\"; keeping it singular is what makes the cognitive synergy possible (every process reasons over the same atoms).\n\nSo Prolog is a backward-tabled reasoning strategy (layer 1) best realized as native tabling over PathMap (layer 2) — not a parallel cognitive architecture with its own store (that would fork the substrate, which is explicitly ruled out). The positive/stratified fragment of that strategy already runs on the forward engine (semi-naive saturation + magic-sets + stratified NAF); the only genuinely distinct residual is WFS recursion-through-negation.","title":"Three orthogonal layers — substrate vs engines vs cognitive processes"},{"category":"section","location":"backends/#MORK-/-ZAM-—-the-substrate-backend-(integrated)","page":"Execution Backends","text":"Every Core atom is a byte-path in a MORK PathMap trie; matching and joins run on the ZAM (Zipper Abstract Machine) — the zipper traversal already implemented in PathMap, not a model to build. By the Mork-theory selectivity theorem, the zipper delivers O(1) candidates to the unifier for selective symbolic joins (where a WAM is Ω(N) per leg). Core exposes the native engine directly through the E1 bridge (mork_unify, mork_apply, mork_rule_rewrite) and the trie-index query space_query_multi — the same path the Pattern Mining native count and the connectome info_flow workload use.\n\nThe MM2 exec-calculus ((exec …)) and the MeTTa-IL lane both lower onto this substrate.","title":"MORK / ZAM — the substrate backend (integrated)"},{"category":"section","location":"backends/#MorkSupercompiler-—-the-optimization-backend-(integrated)","page":"Execution Backends","text":"sc_execute! runs a program through the tier-2 MorkSupercompiler against the space's trie: join-order + Rule-of-64 decomposition (tier-1 plan!), plus opt-in stages — error-bounded approximate rewrite, KBSaturation (seminaive fixpoint — the recursive-closure engine the MeTTa-IL lane uses via saturate=true), magic-sets goal-direction (use_magic_sets — a bottom-up surrogate for top-down SLG tabling that rewrites the rules toward a query so saturation tables only goal-relevant facts; sound for the single-predicate / self-recursive / bound-first fragment, falls back to full closure otherwise), the §6 supercompilation driver, and MM2 lowering. Stage opt-ins live on SCOptions and are reachable identically from both lanes (Direct via supercompile=true, MeTTa-IL via saturate=true).\n\nnote: Materializing, not streaming\nThe supercompiler materializes intermediate join results as _sc_tmp trie atoms — measured ~5–30× slower than streaming (docs/specs/execution_model_architecture.md: 771ms vs 24ms ≈ 32×, 2389ms vs 504ms ≈ 4.7×). It is an optimization/closure layer, not the fast match path. The streaming fast path is lean selective probing on the zipper (ZAM) (space_query_multi / the connectome info_flow), which realizes the MORK selectivity result — O(1) candidates vs a WAM's Ω(N) per leg under Σγ>1 (Mork-theory), an asymptotic advantage, not a fixed factor. Use the supercompiler for saturation and whole-program rewriting, not as a general query accelerator.","title":"MorkSupercompiler — the optimization backend (integrated)"},{"category":"section","location":"backends/#Prolog-—-the-tabled-recursion-backend-(exploration,-not-wired)","page":"Execution Backends","text":"Honest status: Prolog is not integrated into Core. No Prolog code ships in src/. It is a decided architectural role with a validated FFI spike, not a live backend.\n\nThe engine-per-workload analysis assigns Prolog exactly one job: backward, goal-directed, tabled recursion (SLG resolution with well-founded-semantics negation) — the one workload the forward ZAM does not cover. The plan is SWI-Prolog via a libswipl FFI bridge now, phased to native tabling-over-ZAM later, so the dependency is temporary. Explicitly ruled out: compiling everything to Prolog (PeTTa's model) — it would make Core permanently Prolog-bound and SWI is slow at simple predicate calls.\n\nWhat exists today (in the cross-cutting docs/specs/prolog/, not in this repo):\n\nprolog_mork_dual_backend_plan.md — the dual-backend plan and phase-out path.\nSWI-Prolog_10.1.9_ch7_tabling_spec.md — the SLG tabling semantics (the genuine gap).\nspike/ — a runnable FFI spike (swipl_ffi_spike.{pl,jl}, PrologBackend.jl, test_prolog_backend.jl, 5/5) proving the Julia ↔ SWI bridge and the adapter shape.\n\nBefore any wiring, the discipline is measure first — the γ̂ selectivity diagnostic determines which workloads actually need backward tabling versus the forward zipper. That diagnostic is built (MORK/tools/zam_diagnostic.jl — γ̂ = −log_N(|P(p)|/N) with the Σγ̂ regime split, self-checked) but is a standalone REPL tool, not yet wired into any routing decision. Until a measured workload demands it, Prolog stays an exploration.\n\nnote: Prolog as a complementary interim engine (decision in progress)\nThe plan endorses SWI now → native-tabling-over-PathMap later. To use it as a complementary interim engine without forking the substrate, three guardrails: (1) scope to the residual — WFS recursion-through-negation only (the forward engine already covers positive/stratified); (2) marshall, don't mirror — atoms→Prolog terms per query, answers back, drop Prolog state (MORK stays the single source of truth); (3) concrete exit — retire when native WFS passes SWI's conformance set (parity) and γ̂ says the workload doesn't need it, else it acquires permanent lock-in. Highest-value first step is the differential ORACLE, not the engine: wire SWI as ground truth to validate native WFS against (the pattern that de-risked the PLN chainer via PLNDemand.jl), and promote oracle→engine only if a measured WFS workload arrives before native is ready.\n\nnote: Positive-fragment coverage (what the forward engine already tables)\nSemi-naive saturate! (live) + magic-sets goal-direction (opt-in) cover positive recursive closure (transitive closure / ancestor / path) — the bottom-up equivalent of top-down SLG tabling for the positive fragment. Arithmetic/comparison guard premises (< > <= >= == != + - * / %) are now EVALUATED in rule bodies (mirroring GROUNDED_REGISTRY so they bisimulate the MM2 calculus lane): comparisons filter; a 3-arg (op a b c) binds its output. A value-generating rule needs a bounding guard to terminate — an unbounded one is truncated at the round cap with a warning (the pragmatic stand-in for the supercompilation homeomorphic-embedding whistle). What still routes to no engine: negation of any kind (NAF, stratified, or well-founded), relational aggregation, and incremental retraction / TMS — the genuine residual gaps.","title":"Prolog — the tabled-recursion backend (exploration, not wired)"},{"category":"section","location":"backends/#The-Interpreter-as-meta-kernel","page":"Execution Backends","text":"The standard Interpreter is the live evaluator today and the conformance reference (the spec the other lanes bisimulate against). In the target model it shrinks to the ~5% reflective meta-kernel — function/return, type-cast, reflection — while ZAM owns matching and the supercompiler owns whole-program optimization. The end state is not \"zero interpreter\" but a thin reflective tail over engine-routed hot paths.\n\n","title":"The Interpreter as meta-kernel"},{"category":"section","location":"#MeTTaCore.jl","page":"Home","text":"MeTTaCore is the canonical standalone MeTTa interpreter of the CognitiveSubstratesAI stack, running on the MORK byte-trie substrate. Evaluation is eager and single-result; numbers are Float64. Dispatch is three-tier: special forms → grounded operations → rule rewriting.","title":"MeTTaCore.jl"},{"category":"section","location":"#Architecture","page":"Home","text":"Programs (MeTTa-IL rewrites / def-match-emit pipelines / GSLT theories, or direct MM2/MeTTa) enter through the mc_run dual-track entry, lower to the MM2 exec calculus, and run on the MORK / PathMap byte-trie via the ZAM zipper. See Execution Backends and the MeTTa-IL Lane.\n\nflowchart TD\n    subgraph FE[\"Front-ends\"]\n      IL[\"MeTTa-IL: rewrites / def-match-emit / GSLT theories\"]\n      DP[\"Direct: MM2 programs / MeTTa\"]\n    end\n    IL --> MCR[\"mc_run (dual-track entry)\"]\n    DP --> MCR\n    MCR --> MM2[\"MM2 exec calculus\"]\n    MM2 --> MORK[\"MORK / PathMap (ZAM zipper)\"]\n    MORK --> BK[\"Backends: supercompiler / Prolog spike / Tensor Logic (MORKTensorNetworks)\"]\n\nusing MeTTaCore\nspace = new_core_space()\nef = e -> to_sexpr(eval_metta(from_sexpr(e), space))\nregister_all_primitives!(); _register_atom_ops!(ef); load_stdlib!(space)\n\nrun_metta(\"!(+ 2 3)\", space)            # => 5\nrun_metta(\"!(import! &self (library MOSES))\", space)","title":"Architecture"},{"category":"section","location":"#Algorithm-library","page":"Home","text":"Cognitive algorithms live in lib/ as MeTTa, loaded on demand via !(import! &self (library <Name>)):\n\nMOSES — Meta-Optimizing Semantic Evolutionary Search (competent program evolution: representation → knob-building → boolean reduct → getCandidate → scoring → hill-climb → metapopulation search). End-to-end verified to learn boolean functions.\nPLN — Probabilistic Logic Networks core logic.\nECAN — Economic Attention Networks.\nMetaMo — motivational framework.\nActPC-Chem / ActPC-Geom — Active Predictive Coding.","title":"Algorithm library"},{"category":"section","location":"#Design-notes","page":"Home","text":"In-repo design and architecture documents live under docs/ (algorithm-library governance, the primitive-surface audit, the multi-space/connectome architecture, and per-algorithm specs). The cross-cutting research corpus and source-paper map live in the shared CognitiveSubstratesAI/docs repository.\n\nThe full API Reference renders every public symbol via @autodocs.\n\n","title":"Design notes"}]
}
